{
  "routes": [
    { "src": "/estimations", "dest": "/" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}